<unity xmlns="http://schemas.microsoft.com/practices/2010/unity">
    <!--定义Unity配置的命名空间和需要扫描的程序集，指示Unity将会在指定的程序集中查找类型进行注册和依赖注入。-->
    <namespace name="ASPNET.StarterKit.Portal" />
    <assembly name="Portal" />
    <assembly name="Portal.Components" />
    <assembly name="Portal.Components.Data" />
    <assembly name="Portal.Components.Data1" />

    <container>
        <!--连接串，生产环境中需要改为实际的数据源。MultipleActiveResultSets=True 表示可并行处理请求-->
        <!--<instance> 并不仅限于数据库连接字符串的配置，它也可以用于配置任何需要预先定义并注入到容器中以便后续使用的值。例如，可以配置一个配置对象实例、常量、甚至是工厂委托等。在Unity容器中，<instance> 元素主要用于创建那些非接口类型的单例实例，或者在构造其他类型时需要的依赖对象。-->
        <instance name="connectionString" type="string"
                  value="Data Source=(localdb)\MSSQLLocalDB;Integrated Security=True;Initial Catalog=Portal; MultipleActiveResultSets=True" />

        <instance name="testStr" type="string"
                  value="default UnityCfg" />

        <!--PORTAL SECURITY-->
        <!--<register> 标签用于注册一个类型映射关系，指定当应用程序请求指定类型时，容器应该提供哪种类型的实例。-->
        <!--
        以下面这段配置为例，做了以下几件事：

        type="PortalSecurityDbContext": 指定要注册的接口类型或抽象类型。在这个例子中，PortalSecurityDbContext 是一个数据库上下文类（具体代码中为Entity Framework DbContext的子类）。

        mapTo="PortalSecurityDbContext": 指定映射到的实际类型，这里也是PortalSecurityDbContext，说明接口类型和实现类型是同一个类，也就是说，PortalSecurityDbContext既是接口也是实现类。

        <lifetime type="singleton">: 设置了注册类型的生命周期策略为单例（Singleton）。这意味着每当应用程序请求 PortalSecurityDbContext 类型的实例时，容器将始终返回同一个实例。

        <constructor>: 表明要注入依赖项到类的构造函数中。

        <param name="connectionString" dependencyName="connectionString" dependencyType="string" />: 在这个标签内，name="connectionString" 指定了注入到构造函数中的参数名称。dependencyName="connectionString" 表示这个参数值来自之前定义的名为 "connectionString" 的实例，其类型为 string。这就意味着在创建 PortalSecurityDbContext 实例时，Unity将把之前定义的数据库连接字符串注入到它的构造函数中。

        总结来说，这段配置告诉Unity容器，每当应用程序请求 PortalSecurityDbContext 类型的实例时，应当创建一个单例的 PortalSecurityDbContext 实例，并在创建实例时，将其构造函数参数 connectionString 设置为先前定义好的数据库连接字符串实例的值。这样，每次使用 PortalSecurityDbContext 的时候，都能使用同一份数据库连接，同时也避免了硬编码数据库连接字符串。
        -->
        <register type="PortalSecurityDbContext" mapTo="PortalSecurityDbContext">
            <lifetime type="singleton" />
            <constructor>
                <param name="connectionString" dependencyName="connectionString" dependencyType="string" />
            </constructor>
        </register>

        <!--
        
        再以以下这段Unity配置为例，作用为：

        type="IUsersDb": 指定要注册的接口类型。IUsersDb 是一个接口，表示一种特定的服务契约或职责，例如可能包含了对用户数据进行CRUD操作的相关方法。

        mapTo="UsersDb": 映射到实际的实现类型为 UsersDb。这意味着当应用程序请求 IUsersDb 类型的服务时，Unity容器将返回一个 UsersDb 类型的实例来满足这个请求。

        <lifetime type="singleton">: 设置了 UsersDb 类型实例的生命周期策略为单例（Singleton）。这意味着在整个应用程序生命周期内，无论何时请求 IUsersDb 类型的服务，Unity容器都将返回同一个 UsersDb 类型的实例，以此来保证整个应用中用户数据库操作的全局一致性，同时提高性能（避免了每次请求都创建新实例的成本）。

        综上所述，这段配置就是让Unity容器知道如何处理对 IUsersDb 接口的依赖注入请求，当有类需要 IUsersDb 类型的依赖时，容器会返回一个预先创建好的单例 UsersDb 类型实例。这样，开发者只需关注接口 IUsersDb，无需关心具体实现类 UsersDb 的实例化过程，实现了依赖倒置原则和控制反转（IoC）。
        -->
        <register type="IUsersDb" mapTo="UsersDb">
            <lifetime type="singleton" />
        </register>

        <register type="IRolesDb" mapTo="RolesDb">
            <lifetime type="singleton" />
        </register>

        <register type="IPortalSecurity" mapTo="PortalSecurity">
            <lifetime type="singleton" />
        </register>

        <!--PORTAL CONFIGURATION-->

        <register type="PortalCfgDbContext" mapTo="PortalCfgDbContext">
            <lifetime type="singleton" />
            <constructor>
                <param name="connectionString" dependencyName="connectionString" dependencyType="string" />
            </constructor>
        </register>

        <register type="IPortalDb" mapTo="PortalDb">
            <lifetime type="singleton" />
            <constructor>
                <param name="connectionString" dependencyName="connectionString" dependencyType="string" />
            </constructor>
        </register>

        <register type="ITabsDb" mapTo="TabsDb">
            <lifetime type="singleton" />
        </register>

        <register type="IModulesDb" mapTo="ModulesDb">
            <lifetime type="singleton" />
        </register>

        <register type="IModuleDefsDb" mapTo="ModuleDefsDb">
            <lifetime type="singleton" />
        </register>

        <register type="IGlobalsDb" mapTo="GlobalsDb">
            <lifetime type="singleton" />
        </register>


        <!--PORTAL MODULES-->
        <register type="PortalDbContext" mapTo="PortalDbContext">
            <lifetime type="singleton" />
            <constructor>
                <param name="connectionString" dependencyName="connectionString" dependencyType="string" />
            </constructor>
        </register>

        <register type="IAnnouncementsDb" mapTo="AnnouncementsDb">
            <lifetime type="singleton" />
        </register>

        <register type="IContactsDb" mapTo="ContactsDb">
            <lifetime type="singleton" />
        </register>

        <register type="IDiscussionsDb" mapTo="DiscussionsDb">
            <lifetime type="singleton" />
            <constructor>
                <param name="connectionString" dependencyName="connectionString" dependencyType="string" />
            </constructor>
        </register>

        <register type="IDocumentsDb" mapTo="DocumentsDb">
            <lifetime type="singleton" />
        </register>

        <register type="IEventsDb" mapTo="EventsDb">
            <lifetime type="singleton" />
        </register>

        <register type="IHtmlTextsDb" mapTo="HtmlTextsDb">
            <lifetime type="singleton" />
        </register>

        <register type="ILinksDb" mapTo="LinksDb">
            <lifetime type="singleton" />
        </register>

    </container>
</unity>