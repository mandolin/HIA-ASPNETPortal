
0.You need NuGet enabled.

1. Run the scripts in the Setup directory: Portal_CreateDB.sql, Portal_LoadConfig.sql, Portal_LoadData.sql
2. Change the connection strings inside the UnityCfg.xml file
3. Build and Run. Login as admin with guest/guest

PLEASE note that this version is under construction. For a stable version download the realease.

