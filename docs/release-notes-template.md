# 发布说明模板

> 本模板用于人工整理每次 HIA-ASPNETPortal 交付说明。发布说明面向可公开传阅的维护、测试或部署人员，不记录真实连接串、密码、Token、Cookie、证书私钥、生产路径或内部 WorkZone 证据路径。

## 基本信息

| 项 | 内容 |
| --- | --- |
| 版本号 | `0.13.N` |
| 发布名称 | `<发布名称>` |
| 发布日期 | `<yyyy-MM-dd>` |
| 发布类型 | `<内部验证 / 测试交付 / 生产候选 / 正式发布>` |
| 对应提交 | `<短提交号>` |
| 发布包编号 | `<release manifest 中的 ReleaseId>` |
| 适用环境 | `<Dev / Test / Prod / Scan / LegacyIe>` |

## 版本定位

简要说明本次发布解决什么问题、面向什么验证场景，以及是否属于预 1.0 阶段。

示例：

```text
本版本为 P13 产品化交付准备阶段的内部验证版本，重点验证发布包、部署模板、运维证据、文档化准备和发布说明材料，不代表 1.0 正式生产版本。
```

## 主要变更

| 分类 | 说明 |
| --- | --- |
| 功能 | `<本次新增或调整的用户可见功能>` |
| 运维 | `<运行手册、日志、审计、健康检查或证据包变化>` |
| 文档 | `<公开文档、部署说明、测试清单或文档化工具链变化>` |
| 安全 | `<安全 header、凭据、权限、审计或合规边界变化>` |
| 数据库 | `<迁移脚本、兼容矩阵、schema 或数据修复变化>` |

## 验证摘要

| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| 构建 | `<通过 / 条件式通过 / 未执行>` | `<摘要>` |
| 发布包 manifest | `<通过 / 条件式通过 / 未执行>` | `<失败数、警告数>` |
| 运维证据 | `<通过 / 条件式通过 / 未执行>` | `<失败数、Pending 数>` |
| 文档化证据 | `<通过 / 条件式通过 / 未执行>` | `<失败数、Pending 数>` |
| 公开文档门禁 | `<通过 / 未执行>` | `<摘要>` |
| 手工回归 | `<通过 / 部分通过 / 未执行>` | `<摘要>` |

## 发布与部署说明

1. 按 [部署检查清单](deployment-checklist.md) 确认目标环境、外置配置、数据库和回滚责任人。
2. 按 [回滚指南](deployment-rollback-guide.md) 准备文件、配置、数据库和证据回滚材料。
3. 按 [运维运行手册](operations-runbook.md) 完成发布后健康检查、日志维护 dry-run、审计查询和备份提醒。
4. 发布前不得将开发连接串、共享默认密码、真实证书私钥、生产备份或内部证据目录放入发布包。

## 已知限制与延期补证

| 项 | 状态 | 处理 |
| --- | --- | --- |
| 真实 IIS / TLS / 应用池 ACL | `<待补证 / 已补证>` | 需要在目标环境复测。 |
| SQL Server 2016 / 2017 / 2019 | `<待补证 / 已补证>` | 需要对应真实实例验证。 |
| 企业扫描工具 | `<待补证 / 已补证>` | 需要记录扫描工具、规则版本和例外。 |
| HIA consumer / transport | `<待补证 / 已补证>` | 需要等待 HIA 体系运行时接入。 |
| 真实业务签收 | `<待补证 / 已补证>` | 需要业务负责人确认样板流程。 |

## 回滚入口

| 回滚范围 | 入口 |
| --- | --- |
| 文件发布包 | 使用上一版发布包或已确认的文件备份。 |
| 外置配置 | 恢复发布前配置备份。 |
| 数据库 | 使用发布前数据库备份或已审查的回滚脚本。 |
| 运行状态 | 按运行手册复核健康检查、日志和审计。 |

## 发布结论

```text
<填写本次发布是否允许进入下一验证环境、是否需要补证、是否需要回滚或暂缓。>
```
